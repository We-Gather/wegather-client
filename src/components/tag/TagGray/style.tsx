import styled from 'styled-components';

export const TagWrapper = styled.div`
  display: inline-flex;
  margin: 0 0.25rem;
  padding: 0.3125rem 1.25rem;
  align-items: flex-start;
  gap: 0.625rem;
  border-radius: 50px;
  background: #e5e5e5;
`;

export const TagWord = styled.span`
  color: #585858;
  font-size: 0.75rem;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
`;
